{
  "name": "bush-gums-affiliate-curation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static-first Australian beginner overnight hiking gear research and affiliate curation site.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy:check": "node scripts/check-deploy-config.mjs",
    "deploy": "npm run deploy:check && npm run build && npx wrangler deploy",
    "preview": "astro preview",
    "check": "astro check",
    "seo:check": "node scripts/check-seo.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.3.0",
    "@fontsource-variable/geist-mono": "^5.3.0",
    "@fontsource-variable/outfit": "^5.3.0",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@fontsource/outfit": "^5.3.0",
    "opentype.js": "^2.0.0",
    "potrace": "^2.1.8",
    "prettier": "^3.6.2",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.9.2"
  }
}
